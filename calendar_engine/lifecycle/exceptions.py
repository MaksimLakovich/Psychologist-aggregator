"""Исключения для lifecycle-модуля.

Этот модуль является единой точкой для предметных ошибок жизненного цикла события/слота:
    - системное изменение статусов события/слотов;
    - пользовательское изменение статуса (перенос/отмена).
"""


class LifecycleActionValidationError(Exception):
    """Бизнес-ошибка lifecycle-действия над уже созданной встречей или ее слотом.

    Используется, когда:
        - клиент передал некорректные входные данные;
        - выбранный слот уже недоступен;
        - у специалиста нет рабочего правила;
        - создание встречи противоречит текущим бизнес-инвариантам.
    """
