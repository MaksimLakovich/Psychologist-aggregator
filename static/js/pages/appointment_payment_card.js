import { initAddAppointmentAndPaymentCard } from "../modules/add_appointment_payment.js";

document.addEventListener("DOMContentLoaded", () => {

    // 1. Получаем блок для подтверждения записи и добавление платежной карты для списания за сессию
    initAddAppointmentAndPaymentCard();

});
