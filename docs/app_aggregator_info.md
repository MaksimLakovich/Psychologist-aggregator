# 🛠️ Агрегатор системы (фильтрация и ранжирование данных в публичном каталоге психологов)

Задача aggregator: показывать профиль наружу - красиво, фильтровать, искать, ранжировать.

---

## <a id="title1"> 🔑 Основные особенности </a>

- Подбор психологов (фильтрация по methods, topics, price range, experience range, age, gender)
- Ранжирование (алгоритм выдачи) - рандомный вывод карточек психологов в "Публичном каталоге психологов" (is_verified=True, is_active=True)
- Дополнительная scoring-модель (relevance алгоритм):
  - повышение позиции в выводе по "last_login" - т.е. повышение положения психологов, которые чаще работают с приложением
  - повышение позиции в выводе по "rating" + "уровень совпадений интересующих тем клиента в requested_topics и тем психолога в "topics" + аналогичное совпадение в методах - "preferred_methods" и "methods")
- Рекомендации системы (продвижение) / продвинутый boost (подписка)

---

## <a id="title2"> ⚒️ Сервисные вспомогательные функции </a>

### aggregator/paginators.py:

- `PsychologistCatalogPagination(PageNumberPagination)` - класс-пагинатор для API-эндпоинта ***Публичного каталога психологов***.

---

## <a id="title3"> ⚙️ API-функционал </a>


### 1. СЕРИАЛИЗАТОРЫ МОДЕЛЕЙ

### aggregator/_api/serializers.py:

Для **API-эндпоинтов** созданы следующие сериализаторы:

- Публичный каталог психологов:
  - `PublicPsychologistListSerializer` - взаимодействие авторизованного пользователя приложения с **Публичным каталогом психологов** (получение карточек психологов)


### 2. ФИЛЬТРАЦИЯ 

### aggregator/_api/filters.py:

- `PsychologistFilter(filters.FilterSet)` - кастомный FilterSet, который укажет для фильтрации параметр **slug**, а не системный **id**.

    
### 3. КОНТРОЛЛЕРЫ 

### aggregator/_api/views.py:

#### 1) API 

| № | Название контроллера           | Тип (ViewSet / Generic) | Описание функционала (docstring)                                                                                                         | Используемые модели              | Используемые сериализаторы         |
|---|--------------------------------|------------------------|------------------------------------------------------------------------------------------------------------------------------------------|----------------------------------|------------------------------------|
| 1 | **PublicPsychologistListView** | `ListAPIView`          | Взаимодействие авторизованных пользователей с *Публичным каталогом психологов*: отображение карточек психологов / фильтрация / пагинация | `AppUser`, `PsychologistProfile` | `PublicPsychologistListSerializer` |

#### 2) AJAX-запрос (fetch) на специальный API-endpoint

| № | Название контроллера           | Тип (ViewSet / Generic) | Описание функционала (docstring)                                                                | Используемые модели                            |
|---|--------------------------------|------------------------|-------------------------------------------------------------------------------------------------|------------------------------------------------|
| 1 | **MatchPsychologistsAjaxView** | `View`                 | Автоматический запуск фильтрации психологов без кнопки "Далее" по указанным клиентом параметрам | `AppUser`, `PsychologistProfile` + справочники |


### 4. МАРШРУТЫ (РОУТЫ)

### aggregator/_api/urls.py:

#### 1) API 

| № | Эндпоинт                                                                                          | HTTP-методы | Описание функционала                               |
|---|---------------------------------------------------------------------------------------------------|------------|----------------------------------------------------|
| 1 | `/aggregator/api/psychologists/?page=1&topics=vygoranie,stress&methods=simvoldrama&gender=female` | `GET`      | Публичный список психологов (фильтрация/пагинация) |

#### 2) AJAX-запросы (fetch) на моментальное сохранение указанных на html-страницах данных в БД

| № | Эндпоинт                               | HTTP-методы | Описание функционала                                                                                        |
|---|----------------------------------------|------------|-------------------------------------------------------------------------------------------------------------|
| 1 | `/aggregator/api/match-psychologists/` | `GET`      | AJAX-запрос (fetch) для моментальной фильтрации психологов по указанным клиентом критериям на html-странице |

---

## <a id="title4"> 🖥️ WEB-функционал </a>

### 1. ФИЛЬТРЫ

### aggregator/_web/selectors/:

Для **WEB-эндпоинтов** созданы следующие методы для фильтрации:

| № | Модуль                      | Метод                          | Описание функционала (docstring)                                                                                                                                                                                                                 |
|---|-----------------------------|--------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 1 | `psychologist_selectors.py` | **base_queryset()**            | Метод возвращает базовый QuerySet, содержащий всех активных/верифицированных психологов из базы данных                                                                                                                                           |
| 2 | `psychologist_selectors.py` | **filter_by_topic_type()**     | Метод оставляет психологов, у которых в профиле есть хотя бы одна тема (topics) из заданного клиентом *Вида консультации* (individual/couple). Это нужно для того, если клиент указал вид консультации, но не выбрал ни одной темы из этого вида |
| 3 | `psychologist_selectors.py` | **annotate_topic_matches()**   | Метод аннотирует поле matched_topics_count, где подсчитывает количество совпадающих тем из профиля психолога (topics) с темами из профиля клиента (requested_topics)                                                                             |
| 4 | `psychologist_selectors.py` | **annotate_type_topic_count()** | Метод аннотирует поле type_topics_count. Данные о том сколько у каждого психолога указано в его профиле тем из указанного клиентом *Вида консультации*: individual / couple (полезно когда requested_topics пуст)                                |
| 5 | `psychologist_selectors.py` | **filter_by_gender()**         | Метод с жесткой фильтрацией по полу психолога (мужчина/женщина)                                                                                                                                                                                  |
| 6 | `psychologist_selectors.py` | **filter_by_age()**            | Метод с жесткой фильтрацией по возрасту психолога                                                                                                                                                                                                |
| 7 | `psychologist_selectors.py` | **annotate_method_matches()**  | Метод аннотирует поле matched_methods_count, где подсчитывает количество совпадающих методов из профиля психолога (methods) с методами из профиля клиента (preferred_methods)                                                                    |

### 2. СЕРВИСЫ (СКОРИНГ + ИТОГОВАЯ ФИЛЬТРАЦИЯ С РАНЖИРОВАНИЕМ)

### aggregator/_web/services/:

| № | Модуль             | Метод                      | Описание функционала (docstring)                                                                                                                          |
|---|--------------------|----------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------|
| 1 | `scoring.py`       | **topic_score()**          | Метод аннотирует поле topic_score (коэффициент совпадения тем)                                                                                            |
| 2 | `scoring.py`       | **method_score()**         | Метод аннотирует поле method_score (коэффициент совпадения методов)                                                                                       |
| 3 | `scoring.py`       | **apply_final_ordering()** | Финальная сортировка психологов. Логика: 1) topic_score - главный критерий; 2) method_score - вторичный критерий                                          |
| 4 | `filter_service.py` | **match_psychologists()**  | Метод возвращает итоговый QuerySet, содержащий психологов отсортированных по коэффициенту совпадения тем, полу, возрасту и коэффициенту совпадения методов |

---
